apiVersion: apps/v1
kind: Deployment
metadata:
  name: ca-agent
  namespace: ca-system
spec:
  template:
    spec:
      containers:
      - name: ca-agent
        env:
        - name: ALLOWED_ORIGINS
          value: "https://collective-museum.com"
