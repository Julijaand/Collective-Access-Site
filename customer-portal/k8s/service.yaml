apiVersion: v1
kind: Service
metadata:
  name: customer-portal
  namespace: ca-system
  labels:
    app: customer-portal
spec:
  selector:
    app: customer-portal
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  type: ClusterIP
